#ifndef __CONFIG_HH__
#define __CONFIG_HH__

#define	MONGODB_URI		("localhost")
#define	MONGODB_COL		("CamelBabel.users")

#define	FIELD_ID		("_id")
#define	FIELD_USERNAME		("username")
#define	FIELD_PASSWORD		("password")
#define	FIELD_CONTACTS		("contacts")
#define	FIELD_MESSAGES		("messages")
#define	FIELD_MESS_FROM		("from")
#define	FIELD_MESS_MESS		("message")
#define	FIELD_MESS_DATE		("date")

#define	BABEL_LOGOUT_STATE	(0)

#endif
